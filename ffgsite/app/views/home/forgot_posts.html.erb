<% if @user != nil %> <!-- if we have an ID parameter that refers to a real user -->
  Did you forget to submit a text message today with something that you thought about posting on Facebook but did not?<br><br>
  <%= link_to('Yes',"/posts?id=" << @user.link << ";date=" << params[:date] << ";type=p;uc=c", :method => "get") %>
  <%= link_to('No',"/home/posted_on_fb?id=" << @user.link << ";date=" << params[:date] << ";type=p;uc=c", :method => "get") %>
<% end %>

